# ============================================================================
# Compliance Automation Platform — Python Dependencies
# ============================================================================
# Install:  pip install -r requirements.txt
# Dev:      pip install -r requirements.txt -r requirements-dev.txt
# ============================================================================

# ── Core / Orchestration ────────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0
structlog>=24.0.0
httpx>=0.27.0
tenacity>=9.0.0

# ── Azure Identity & Key Vault ──────────────────────────────────────────────
azure-identity>=1.17.0
azure-keyvault-secrets>=4.8.0

# ── Azure Cosmos DB (audit ledger, deduplication registry) ──────────────────
azure-cosmos>=4.7.0

# ── Azure Storage (ADLS Gen2 / Blob) ────────────────────────────────────────
azure-storage-blob>=12.22.0

# ── Azure Document Intelligence (Form Recognizer) ──────────────────────────
azure-ai-formrecognizer>=3.3.0

# ── Azure AI Search (vector indexing) ───────────────────────────────────────
azure-search-documents>=11.5.0

# ── LLM / Embedding ────────────────────────────────────────────────────────
openai>=1.40.0
tiktoken>=0.7.0

# ── PDF / Document Processing ──────────────────────────────────────────────
PyPDF2>=3.0.0
reportlab>=4.2.0
lxml>=5.2.0

# ── Observability ───────────────────────────────────────────────────────────
opentelemetry-api>=1.26.0
opentelemetry-sdk>=1.26.0
opencensus-ext-azure>=1.1.0

# ── Utilities ───────────────────────────────────────────────────────────────
tzdata>=2023.3

# ── Knowledge Graph / Agentic Intelligence Layer ─────────────────────────────
networkx>=3.3.0            # ComplianceOntology — regulatory graph engine
PyYAML>=6.0.1              # regulations.yaml seed loader
scikit-learn>=1.4.0        # AnomalyDetector (IsolationForest)
pytesseract>=0.3.10        # ScreenshotOCRVerifier — OCR on evidence screenshots
Pillow>=10.3.0             # Image processing for OCR pipeline
